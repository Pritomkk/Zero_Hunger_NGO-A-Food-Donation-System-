@model IEnumerable<Assignment_Zero_Hunger.EF.FoodDistributionByEmployee>

@{
    ViewBag.Title = "ViewDistributionFoodByEmploye";
}

<h2>ViewDistributionFoodByEmploye</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Distribution_Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmpId)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Distribution_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmpId)
            </td>

        </tr>
    }

</table>
<div>
    @Html.ActionLink("Back ", "NGOAdminDash")
</div>
